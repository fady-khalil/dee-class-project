import { StyleSheet } from "react-native";

export const GlobalStyle = StyleSheet.create({
  container: {
    paddingLeft: 10,
    paddingRight: 10,
  },
  safeAreaView: {
    flex: 1,
  },
});
